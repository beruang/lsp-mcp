import { LspClient } from "./LspClient.js";

export class LspClientManager {
  getClientForLanguage(lang: string): LspClient | null {
    // Stub: returns null. Phase-3 fills the registry.
    return null;
  }
}