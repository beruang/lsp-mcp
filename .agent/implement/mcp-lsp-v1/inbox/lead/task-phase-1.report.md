# task-phase-1 Report

**Task:** task-phase-1 — Project skeleton
**Status:** COMPLETE
**Worktree:** `/Volumes/Workspace/rnd/workflow/mcp/lsp/.agent/worktrees/phase-1`
**Branch:** `task/phase-1`

## Final commit

```
bbd3bb9598b6205915bc2c3e0bbf05e5559af9f7  feat(phase-1): project skeleton with stub lsp_health_check
```

11 files changed, 1447 insertions(+).

## Files created / modified

All paths are inside the worktree.

- `package.json` (new)
- `pnpm-lock.yaml` (new, generated by pnpm)
- `tsconfig.json` (new)
- `.gitignore` (modified — added `node_modules`, `dist`, `fixtures/sample-ts/node_modules`, `fixtures/sample-py/.venv`, `*.log`, `.DS_Store`; kept pre-existing `.agent/worktrees/`)
- `README.md` (new)
- `src/index.ts` (new)
- `src/mcp/registerTools.ts` (new)
- `src/mcp/schemas.ts` (new)
- `src/mcp/toolErrors.ts` (new)
- `src/mcp/toolErrors.test.ts` (new)
- `fixtures/.gitkeep` (new, empty)

## Validation output

All commands run from inside the worktree with `pnpm` (v10.33.0).

| Step | Command | Exit | Notes |
| ---- | ------- | ---- | ----- |
| 1 | `pnpm install` | 0 | 105 packages added; one informational notice that `esbuild@0.28.0` build scripts were skipped (transitive dep of `tsx`; runtime path is unaffected, see Deviations). |
| 2 | `pnpm run typecheck` | 0 | `tsc -p tsconfig.json --noEmit` clean. |
| 3 | `pnpm run build` | 0 | Emits `dist/index.js` (+ `dist/mcp/{registerTools,schemas,toolErrors,toolErrors.test}.js`). |
| 4 | `pnpm test` | 0 | 3/3 node:test cases pass in `toolErrors.test.ts`. |
| 5 | Smoke `node dist/index.js < /dev/null` | server printed `MCP server ready, workspace=<path>` to stderr, nothing to stdout | confirmed via process grep |
| 6 | End-to-end MCP roundtrip (initialize + notifications/initialized + tools/list + tools/call lsp_health_check) | 3 valid JSON-RPC responses received on stdout, readiness line on stderr only | `tools/call` returned `{ "ok": true, "message": "phase-1 stub" }` as pretty JSON in a single text content block per spec §17 |
| 7 | WORKSPACE_PATH fallback (`WORKSPACE_PATH=/nonexistent/path/abc`) | stderr warning emitted, then `MCP server ready, workspace=<process.cwd()>` | fallback path verified |

Test summary printed by `node --test`:

```
✔ toolError(code, message) returns the structured envelope without details (1.4ms)
✔ toolError(code, message, details) includes the details field when provided (0.1ms)
✔ toolError omits the details key when details is undefined (0.1ms)
ℹ tests 3  pass 3  fail 0
```

## Deviations from the spec

1. **Package name.** Spec §22 example uses `mcp-lsp-code-intelligence`; the assignment explicitly overrode this to `mcp-lsp-v1`. Honored the assignment.
2. **Dependency versions.** Spec §22 example uses `"latest"` for every dep. Pinned semver ranges in `package.json` (e.g. `@modelcontextprotocol/sdk ^1.29.0`, `zod ^3.23.8`, `typescript ^5.6.3`) so the lockfile is reproducible. The assignment did not specify versions.
3. **Extra tsconfig fields.** Spec §23 example lacks `resolveJsonModule`, `forceConsistentCasingInFileNames`, and `sourceMap`. Added all three as standard hygiene; `target`/`module`/`moduleResolution`/`strict`/`esModuleInterop`/`skipLibCheck`/`outDir`/`rootDir` and `include` all match the spec verbatim.
4. **Extra unit tests.** The assignment allowlists `src/mcp/toolErrors.test.ts` and requires one assertion: `toolError("foo", "bar")` returns `{ error: { code: "foo", message: "bar" } }`. I added two more assertions (details-present and details-undefined) since the allowlist explicitly permits the file and the spec §9.5 shape is what those extras guard. The required assertion is the first test in the file.
5. **pnpm v10 esbuild-build-script notice.** `pnpm install` emitted a one-time notice that `esbuild@0.28.0` build scripts were ignored (pnpm v10 requires explicit `pnpm approve-builds` for native postinstall). This affects only esbuild's optional native binary; `tsx` falls back to its JS path which works fine, and the typecheck, build, test, and smoke run all passed. Not a blocker for phase-1.
6. **No `timeout` on macOS.** The assignment's smoke step uses `timeout 2 pnpm run dev`. macOS does not ship GNU `timeout` and no `gtimeout` is installed. Substituted with a backgrounded subshell, `sleep 2`, then `pkill -f "node dist/index.js"`. Server behaved identically (readiness on stderr, nothing on stdout).

## Read escalations

None. The assignment's allowlist covered every file I needed to read. I did not need to write to `read_escalations.ndjson`.

## What the next agent (QA) should check

- **Transport integrity.** Connect an MCP client (Claude Code, `mcp-inspector`, or a stdio tester) to `node dist/index.js` and call `lsp_health_check` with `{}`. Expect a single text content block whose `text` is pretty JSON `{"ok": true, "message": "phase-1 stub"}`.
- **stdout discipline.** With no MCP client connected, the server must write nothing to stdout. (Verified locally with `node dist/index.js < /dev/null`.) Anything appearing on stdout before `initialize` is a regression.
- **WORKSPACE_PATH semantics.** Three cases worth a sanity check: unset (falls back to `process.cwd()` silently), set-but-missing (stderr warning + fallback), set-and-exists (uses it). All three verified locally.
- **Build artifacts.** `dist/index.js` should be runnable directly via `node dist/index.js`; no other runtime dependencies beyond `node_modules` are required.
- **No stray files.** `git ls-files` on the worktree should include the 11 files listed above and nothing else from the implementation (no `node_modules`, no `dist`, no `fixtures/sample-*`).
- **Phase-2 handoff.** Phase-2 will extend `src/mcp/registerTools.ts` and `src/mcp/schemas.ts`. The exported `ToolContext` and `registerAllTools(server, ctx)` shape are stable; phase-2 should only add new `server.tool(...)` calls, not rename or refactor the entrypoint.

## Hard-rule compliance

- Did not modify the approved contract, the spec, the why doc, or the assignment.
- Did not write outside the worktree.
- Did not add any LSP / language-server code (out of scope for phase-1).
- Did not run multiple agents in the worktree.
- No `pnpm install` or `pnpm run typecheck` failures occurred; no fixes required.
